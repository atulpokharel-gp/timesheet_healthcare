import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function PatientSignaturesPage() {
  return (
    <DashboardShell
      title="Patient Weekly Signatures"
      subtitle="Patient signature workflow for pending weekly forms surfaced from warning deep links (scaffold)."
    />
  );
}
