import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function AdminBillingPage() {
  return (
    <DashboardShell
      title="Billing Outputs"
      subtitle="Billing-ready output generation from approved records and payer rule mappings (scaffold)."
    />
  );
}
