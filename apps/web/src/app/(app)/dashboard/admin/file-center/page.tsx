import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function AdminFileCenterPage() {
  return (
    <DashboardShell
      title="Import / Export Center"
      subtitle="Import parser jobs and outbound export orchestration for forms and finance outputs (scaffold)."
    />
  );
}
