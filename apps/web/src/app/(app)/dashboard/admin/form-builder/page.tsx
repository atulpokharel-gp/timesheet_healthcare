import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function AdminFormBuilderPage() {
  return (
    <DashboardShell
      title="Payer Form Builder"
      subtitle="Payer-specific form template configuration and printable PDF layout management (scaffold)."
    />
  );
}
