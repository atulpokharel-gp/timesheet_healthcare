import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function AdminSchedulingPage() {
  return (
    <DashboardShell
      title="Scheduling"
      subtitle="Patient and caregiver scheduling with assignment and authorization continuity checks (scaffold)."
    />
  );
}
